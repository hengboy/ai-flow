# 实施计划：{需求名称}

> 创建日期：{YYYY-MM-DD}
> 创建时间：{HH:MM:SS}
> 需求简称：{需求简称}
> 需求来源：{需求文档/口头描述/Jira 等}
> 执行范围：single_repo
> 工作区清单：无（单仓模式；若为 workspace 模式，请写 `.ai-flow/workspace.json`）
> 状态文件：`.ai-flow/state/{需求简称}.json`
> 文档角色：本文件仅记录实施证据与执行步骤；流程状态以 JSON 状态文件为准。
> 状态文件约束：`.ai-flow/state/{需求简称}.json` 只能使用 `flow-state.sh` 的固定 schema；不得在 plan 中设计 `requirement_key`、`status`、`steps`、`verification_results`、`change_register` 等自定义字段。
> 执行约定：使用 `/ai-flow-plan-coding` 按 Step 顺序执行；每个 Step 内的动作使用 `- [ ]` 复选框追踪进度。
> 验证约定：代码行为变更默认使用测试优先顺序；完成前必须运行本计划列出的验证命令并记录结果。
> 规则标识：`intake`、`plan-writing`、`file-boundary`、`step-run`、`test-first`、`verify-before-done`、`change-register`

## 1. 需求概述

**目标**：{一句话说明要交付什么能力}

**背景**：{简要描述业务背景、目标用户、预期效果}

**原始需求（原文）**：
{原始需求原文}

**非目标**：{明确本次不做什么，避免执行阶段扩范围}

## 2. 技术分析

### 2.1 涉及模块

| 模块 | 仓库 | 职责 | 变更类型 |
|------|------|------|----------|
| {module} | {repo_id，单仓模式写 -} | {做什么} | 新增/修改 |

### 2.2 数据模型变更

{新增/修改的表结构、字段、索引等；不涉及数据库变更时写"不涉及数据库变更"}

### 2.3 API 变更

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
{不涉及 API 变更时写"无新增/修改接口"}

### 2.4 依赖影响

{是否引入新依赖、是否影响现有接口}

### 2.5 文件边界总览

| 文件 | 仓库 | 操作 | 职责 | 对应步骤 |
|------|------|------|------|----------|
| `{file_path}` | {repo_id，单仓模式写 `-`} | Create/Modify/Test | {这个文件负责什么} | Step N |

### 2.6 高风险路径与缺陷族

| 高风险能力/路径 | 影响面 | 典型失效模式 | 对应缺陷族 | 必须覆盖的验证方式 |
|----------------|--------|--------------|------------|--------------------|
| {例如：SQL 查询链路} | {影响哪些接口/流程/数据} | {例如：条件遗漏、映射错误、结果集偏差} | {缺陷族名称} | {test-compile / 单测 / Mapper / 集成 / build / 人工验证} |

## 3. 实施步骤

### Step 1: {步骤标题}

**目标**：{这一步完成后系统具备什么能力}

**文件边界**：
- Create: `{file_path}` — {新文件职责；没有则删除本行}
- Modify: `{file_path}` — {修改点；没有则删除本行}
- Test: `{file_path}` — {测试覆盖什么；没有则删除本行}

**本轮 review 预期关注面**：
- {本步完成后 review 必须重点检查的缺陷族、关键路径和回归面}

**前置阅读**：
- `{file_path}` — {为什么读，了解什么；仅在需要参考现有代码范式、接口调用方或配置约束时保留}

**执行动作**：
- [ ] **1.1 写失败用例**
  - 文件：`{test_path}`
  - 场景：{输入、操作、断言点}
  - 预期：当前实现下失败，失败原因是 `{明确失败原因}`

- [ ] **1.2 运行失败验证**
  - 命令：`{exact_test_command}`
  - 预期：FAIL，错误信息包含 `{expected_failure_message}`

- [ ] **1.3 实现最小改动**
  - 文件：`{source_path}`
  - 改动：{具体函数、类型、配置、分支逻辑、错误处理和边界条件}
  - 约束：{兼容性、权限、事务、性能或回退要求}

- [ ] **1.4 运行通过验证**
  - 命令：`{exact_test_command}`
  - 预期：PASS

- [ ] **1.5 本步自检**
  - 命令：`git diff -- {changed_paths}`
  - 确认：无计划外文件、无占位符、无临时日志、无未说明行为变化

**本步验收**：
- [ ] {可观察验收条件 1}
- [ ] {可观察验收条件 2}

**本步关闭条件**：
- {修复/实现完成前必须通过的验证与证据，例如 test-compile、目标单测、Mapper 验证、报告补录}

**阻塞条件**：
- 如果 {具体阻塞条件}，停止执行并向用户确认，不猜测实现。

### Step 2: {步骤标题}

**目标**：{这一步完成后系统具备什么能力}

**文件边界**：
- Modify: `{file_path}` — {修改点}
- Test: `{file_path}` — {测试覆盖什么}

**本轮 review 预期关注面**：
- {本步完成后 review 必须重点检查的缺陷族、关键路径和回归面}

**前置阅读**：
- `{file_path}` — {为什么读，了解什么；没有则删除本段}

**执行动作**：
- [ ] **2.1 写失败用例**
  - 文件：`{test_path}`
  - 场景：{输入、操作、断言点}
  - 预期：当前实现下失败，失败原因是 `{明确失败原因}`

- [ ] **2.2 运行失败验证**
  - 命令：`{exact_test_command}`
  - 预期：FAIL，错误信息包含 `{expected_failure_message}`

- [ ] **2.3 实现最小改动**
  - 文件：`{source_path}`
  - 改动：{具体函数、类型、配置、分支逻辑、错误处理和边界条件}
  - 约束：{兼容性、权限、事务、性能或回退要求}

- [ ] **2.4 运行通过验证**
  - 命令：`{exact_test_command}`
  - 预期：PASS

- [ ] **2.5 本步自检**
  - 命令：`git diff -- {changed_paths}`
  - 确认：无计划外文件、无占位符、无临时日志、无未说明行为变化

**本步验收**：
- [ ] {可观察验收条件 1}

**本步关闭条件**：
- {修复/实现完成前必须通过的验证与证据，例如 test-compile、目标单测、Mapper 验证、报告补录}

**阻塞条件**：
- 如果 {具体阻塞条件}，停止执行并向用户确认，不猜测实现。

## 4. 测试计划

### 4.1 单元测试

- [ ] {需要测试的类/方法}
- [ ] {需要覆盖的边界场景}
- {按项目现有测试框架补充测试，例如 JUnit、pytest、Vitest 等}

### 4.2 集成测试

- [ ] {需要测试的接口/流程}

### 4.3 回归验证

- [ ] {完整回归命令，例如 npm test、pytest、go test ./...、bash tests/run.sh}
- [ ] {必要的手工验证步骤；没有则删除}

### 4.4 定向验证矩阵

| 缺陷族 | 目标风险路径 | 定向验证命令 | 验证类型 | 通过标准 |
|--------|--------------|--------------|----------|----------|
| {缺陷族名称} | {对应能力/链路} | `{exact_command}` | {test-compile / 单测 / Mapper / 集成 / build / 人工验证} | {如何判定关闭} |

## 5. 风险与注意事项

- {可能的坑、需要特别注意的边界情况}

## 6. 验收标准

- [ ] {可验证的验收条件 1}
- [ ] {可验证的验收条件 2}

## 7. 需求变更记录

| 时间 | 变更描述 | 确认方式 |
|------|----------|----------|
| {YYYY-MM-DD HH:MM} | {执行过程中新增或调整的需求；无则保留空表} | {用户确认/文档同步/其他} |

## 8. 计划审核记录

### 8.1 当前审核结论

- 审核状态：待审核
- 与原始需求一致性：待审核
- 是否允许进入 `/ai-flow-plan-coding`：否
- 当前审核轮次：0
- 审核引擎/模型：待审核

### 8.2 偏差与建议

- 待审核

### 8.3 审核历史

- 第 0 轮：初始化 draft，待审核。
