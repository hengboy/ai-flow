你是实施计划修订人。请根据失败的计划审核意见重写这份实施计划，使其重新对齐原始需求并可再次送审。

原始需求：
__AI_FLOW_REQUIREMENT__

当前 plan：
__AI_FLOW_PLAN_CONTENT__

失败审核意见：
__AI_FLOW_REVIEW_ITEMS__

修订要求：
1. 输出完整的 plan Markdown，而不是片段
2. 优先修正与失败意见直接相关的章节；不要擅自扩 scope
3. 保留现有的总体结构、`Step ID` 风格和测试闭环风格
4. 不要删除 `## 8. 计划审核记录`；脚本会保留其中历史，请不要把它改写成其他结构
5. `## 1. 需求概述` 中必须保留 `**原始需求（原文）**`
6. 保持 `.ai-flow/state/...` 仍然只由 `flow-state.sh` 管理
7. 直接输出完整 Markdown，不要附加解释文字
8. 如果审核意见涉及跨仓 plan，修正后必须确保：每个"涉及模块"和"文件边界总览"表格的 `仓库` 列正确填写、跨仓文件路径使用 `repo_id/path/to/file`、Git 命令使用对应仓库的 `git -C <repo_path>` 前缀。单仓 plan 的 `仓库` 列填写 `owner`。
